<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        form(action='/create-user' method='post')
            div.form-group
                label(for='exampleInputEmail1') Username
                input#exampleInputEmail1.form-control(type='text' aria-describedby='emailHelp' placeholder='Enter Username' name = 'username')
                small#emailHelp.form-text.text-muted We&apos;ll never share your email with anyone else.
            div.form-group
                label(for='password') Password
                input#password.form-control(type='password' placeholder='Password' name = 'password')
            div.form-group
                label(for='desc') Description
                input#password.form-control(type='text' placeholder='Description' name = 'desc')
            
            button.btn.btn-primary(type='submit') Submit
        
        ul      
            each i in users
                form(name='view'+i.id action='/indiv-user' method='POST')
                    input(type='hidden' name='individ' value=i.id)
                    input(type='submit' name='view-btn' value= i.username class='btn btn-danger')
            else
                li There are no values
            
