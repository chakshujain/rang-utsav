<!DOCTYPE html>
html(lang="en")
    head
        include ./../head.pug
        link(rel="stylesheet", href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css")
    body
        include ./../header.pug
        main#main-container.container
            ul#users-container  
                h3(class='form-text') Click Any username to Upload Their Photos
                each user in users
                    li
                        a(href="/admin/manage?user=" + user.username class='btn btn-primary') #{user.username}  
                else
                    li There are no values
            
            
            form(action='admin/new-user' method='post')
                h3 Create New User Account
                div.form-group
                    label(for='exampleInputEmail1') Username
                    input#exampleInputEmail1.form-control(type='text' aria-describedby='emailHelp' placeholder='Enter Username' name = 'username')
                    small#emailHelp.form-text.text-muted Make sure username you are entering is unique
                div.form-group
                    label(for='password') Password
                    input#password.form-control(type='password' placeholder='Password' name = 'password')
                div.form-group
                    label(for='desc') Description
                    input#password.form-control(type='text' placeholder='Description' name = 'desc')
                div.form-group
                    input#super-user(type='checkbox' name='superuser' value='superuser')
                    label(for='superuser') Make super user
                
                button.btn.btn-primary(type='submit') Submit
            
            
            
